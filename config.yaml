# ----- Configuración del scraper -----

# Correo que viaja en el User-Agent para que el sitio pueda contactarte.
contacto: "contacto"

# Segundos mínimos entre solicitudes (cortesía). No bajar de 1.0.
seg_entre_solicitudes: 1.0

# Cuándo visitar la página de detalle de un aviso nuevo:
#   nunca     -> solo sitemap (cero solicitudes extra)
#   faltantes -> solo avisos sin caption en el sitemap (recomendado)
#   todos     -> todos los avisos nuevos (descripciones completas)
detalle: faltantes

# Segunda fuente: las páginas de categoría del índice (sitemap_grupos_bienesraices.xml).
# El sitemap de novedades trae ~893 avisos; sumando el índice se cubre el catálogo
# completo (~2,332), deduplicado por id de aviso. Cada categoría se lee con UNA
# solicitud: la página incrusta el catálogo completo (K_Avisos) en un JSON.
usar_indice: true
indice:
  # Mínimo de categorías leídas por completo para confiar en el catálogo y
  # calcular bajas. Si la cobertura cae por debajo, esa corrida OMITE las bajas
  # (evita dar de baja avisos solo porque su página de categoría falló con 403).
  umbral_cobertura: 0.8

  # Visitar el detalle de cada aviso NUEVO del índice (~1 solicitud c/u), no solo
  # la "cola" sin precio. A la cola (solo-id, página 2+, sin precio, zona del slug
  # contaminada) le añade precio —la hace visible en el tablero— y corrige
  # zona/colonia desde el og:title (fiable). A los "ricos" (página 1, ya con
  # precio y zona fiable de ZonMun) solo les aporta la descripción libre del
  # vendedor, que el índice no trae; el detalle nunca pisa su precio/zona ya
  # fiables. Solo aplica a ALTAS nuevas del día, no al catálogo completo —el
  # costo en solicitudes lo marca cuántos avisos nuevos aparecen, no el inventario
  # total. Valores:
  #   no     -> no visitar detalle (comportamiento anterior)
  #   venta  -> solo ventas nuevas
  #   todos  -> ventas y rentas nuevas
  enriquecer_cola: todos

# Descarga de imágenes (ver download_photos.py). Mantener en false salvo que
# cuentes con autorización por escrito del sitio o asumas conscientemente el
# riesgo: las fotos son obras protegidas según el Aviso Legal del sitio.
descargar_fotos: false
